using System;

namespace SimplePizzaApp.Data
{
    public static class Configuration
    {
        public const string ConnectionString = "Server=localhost;database=simple-pizza-app;user=root;password=";
    }
}

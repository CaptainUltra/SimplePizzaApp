@model SimplePizzaApp.Web.Models.Ingredient.ShowIngredientViewModel

@{
    ViewData["Title"] = "Подробности за съставка";
}

<h3>Подробности за съставка</h3>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            №
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Id)
        </dd>
        <dt class="col-sm-2">
            Име
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            Последна промяна
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.UpdatedAt)
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Промяна", "Edit", new { id = Model.Id }) |
    <a asp-action="Index">Обратно към списъка</a>
</div>
